Metadata-Version: 2.4
Name: mtg-microsoft-auth
Version: 0.1.0
Summary: Windows-first Microsoft Graph auth foundation for Midtown dev toys
Author-email: Thomas Bray <thomas.bray@midtown.net>
License: MIT
Project-URL: Homepage, https://github.com/Midtown-Technology-Group/mtg-microsoft-auth
Project-URL: Repository, https://github.com/Midtown-Technology-Group/mtg-microsoft-auth
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: msal[broker]>=1.29; platform_system == "Windows"
Requires-Dist: msal>=1.29; platform_system != "Windows"
Requires-Dist: httpx>=0.27.0
Requires-Dist: pydantic>=2.0
Provides-Extra: windows
Requires-Dist: msal_extensions>=1.0; extra == "windows"
Provides-Extra: dev
Requires-Dist: pytest>=7.0; extra == "dev"
Requires-Dist: black>=23.0.0; extra == "dev"
Requires-Dist: ruff>=0.1.0; extra == "dev"

# MTG Microsoft Auth

Windows-first Microsoft Graph auth helpers for Midtown dev toys.

## Features

- `msal` public client authentication
- Windows broker runtime for WAM-first sign-in
- Windows DPAPI-backed token cache via `msal_extensions` when available
- WAM/broker-first login flow on Windows
- Interactive browser and device-code fallbacks
- Thin authenticated Graph client with retry and pagination helpers

## Install

```powershell
python -m pip install -e .[dev]
```

## Usage

```python
from mtg_microsoft_auth import AuthConfig, AuthMode, GraphAuthSession, GraphClient

config = AuthConfig(
    client_id="00000000-0000-0000-0000-000000000000",
    tenant_id="11111111-1111-1111-1111-111111111111",
    scopes=["Tasks.Read", "Tasks.ReadWrite"],
    mode=AuthMode.AUTO,
    cache_namespace="todo-cli",
)

session = GraphAuthSession(config)
client = GraphClient(session)
payload = client.get("/me")
```
